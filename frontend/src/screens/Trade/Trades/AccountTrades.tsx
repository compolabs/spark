import styled from "@emotion/styled";
import React from "react";

interface IProps {}

const Root = styled.div`
  display: flex;
  flex-direction: column;
`;

const AccountTrades: React.FC<IProps> = () => {
  return <Root>AccountTrades</Root>;
};
export default AccountTrades;
