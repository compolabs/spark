import styled from "@emotion/styled";
import React from "react";

interface IProps {
}

const Root = styled.div`
  display: flex;
  flex-direction: column;
`;

const MarketTrades: React.FC<IProps> = () => {
    return <Root>MarketTrades</Root>;
}
export default MarketTrades;
