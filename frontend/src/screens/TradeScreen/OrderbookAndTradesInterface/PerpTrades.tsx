import styled from "@emotion/styled";
import React from "react";

interface IProps {}

const Root = styled.div`
	display: flex;
	flex-direction: column;
`;

const PerpTrades: React.FC<IProps> = () => {
	return <Root>PerpTrades</Root>;
};
export default PerpTrades;
