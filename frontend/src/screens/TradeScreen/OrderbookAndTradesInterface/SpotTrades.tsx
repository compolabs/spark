import styled from "@emotion/styled";
import React from "react";

interface IProps {}

const Root = styled.div`
	display: flex;
	flex-direction: column;
`;

const SpotTrades: React.FC<IProps> = () => {
	return <Root>SpotTrades</Root>;
};
export default SpotTrades;
